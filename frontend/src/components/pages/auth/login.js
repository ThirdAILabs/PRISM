import UserService from "../../../services/userService";

const Login = () => {
    // UserService.doLogout();
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
}

export default Login;